# Ignora file di log e stato generati localmente
logs/
# Ignora il file di savepoint per join a dominio
logs/JoinDomainState.txt
# Ignora impostazioni locali di VS Code
.vscode/
# Impostazioni locali Claude Code (per-utente, non versionate)
.claude/settings.local.json
